[TreeGeneration]
# Tree structure parameters for very repetitive and large trees using copy-paste approach
max_branching_factor = 26
repetition_probability = 0.6
# Depth range for subtrees that can be copied
min_subtree_depth = 3
max_subtree_depth = 20
# Maximum number of nodes (set high for large trees)
max_nodes = 10000

# Alphabet configuration
alphabet_type = letters
alphabet_size = 26

# Verbose output (disabled for performance)
verbose = false

# Random seed for reproducible generation
seed = 42

# Number of trees to generate
tries = 1

# Enable parallel processing (true/false)
parallel = true

# Output configuration
output_directory = ./generated_trees/time_exp
